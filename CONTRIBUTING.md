# 参与开发

先阅读 [README](README.md) 和 [开发说明](docs/development.md)。使用 `scripts/build.sh` 构建生产应用，提交前运行 `scripts/test.sh`；涉及首帧生命周期时运行 `scripts/test.sh --native`。

保持改动范围明确。修复时说明触发条件、前后行为和实际验证结果；区分逻辑检查、编译通过和真实硬件验收。涉及屏幕权限、Esc、全屏或会议避让时，请说明系统版本及机型。

不要提交桌面截图、原始参考视频、录屏、个人日志、绝对本机路径、凭据、签名证书或生成的应用安装包。复现素材优先使用生成图形。不要通过关闭系统安全校验来绕过权限或分发问题。

构建与 CI 不应读取用户屏幕、访问传感器、开启会议、上传诊断或调用付费服务。公开二进制需要单独验证签名、公证和打包范围。
